"""This module contains an implementation of a trigram algorithm."""

BOOK_LIST = ['I', 'wish', 'I', 'may', 'I', 'wish', 'I', 'might']


def two_words():
    """Take a list and return a string of the first two indices."""
    return str(BOOK_LIST[0:2])
